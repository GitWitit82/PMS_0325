export type WorkflowRouteContext = {
  params: {
    workflowId: string;
  };
};

export type PhaseRouteContext = {
  params: {
    phaseId: string;
  };
};

export type TaskRouteContext = {
  params: {
    taskId: string;
  };
}; 